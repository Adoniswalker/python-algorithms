
function ranking(k, scores){
    scores.sort(function(a, b){return b - a});
    let position = 0;
    let count = 0;
    let pre = scores[0]
    var sum = 0;
    for (var i = 0; i < a.length; i++) {
        sum + = a[i];
    }
}
k = 3
scores = [100, 50, 50, 25]
ranking(k, scores)